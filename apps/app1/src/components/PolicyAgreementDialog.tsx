import BaseFullDialog from "@repo/ui/BaseFullDialog";

const PolicyAgreementDialog = ({}) => {
  return (
    <>
      <BaseFullDialog open={false}></BaseFullDialog>
    </>
  );
};

export default PolicyAgreementDialog;
